<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jun.myproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ButtonsActivity"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name=".Grocery"
            android:label="@string/string_grocery">
        </activity>

        <activity
            android:name=".Gasoline"
            android:label="@string/string_gas">
        </activity>

        <activity
            android:name=".Rent"
            android:label="@string/string_Rent_mortage">
        </activity>

        <activity
            android:name=".Electricity"
            android:label="Electricity">
        </activity>

        <activity
            android:name=".Entertainment"
            android:label="Entertainment">
        </activity>

        <activity
            android:name=".Loans"
            android:label="Loans">
        </activity>

        <activity
            android:name=".Maintenance"
            android:label="Maintenance">
        </activity>

        <activity
            android:name=".Restaurant"
            android:label="Restaurant">
        </activity>

        <activity
            android:name=".Insurance"
            android:label="Insurance">
        </activity>

    </application>

</manifest>